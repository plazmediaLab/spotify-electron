import {} from '../types';

export default function AppReducer(state, action) {
  switch (action.type) {
    // case AUTH_LOGIN:
    //   return {
    //     ...state,
    //     user: action.payload
    //   };

    default:
      return state;
  }
}
